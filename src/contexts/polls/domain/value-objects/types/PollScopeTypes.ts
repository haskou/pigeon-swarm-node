import { PollScopeTypeValue } from './PollScopeTypeValue';

export const pollScopeTypes: Record<string, PollScopeTypeValue> = {
  COMMUNITY_CHANNEL: 'community_channel',
  GROUP_CONVERSATION: 'group_conversation',
};
